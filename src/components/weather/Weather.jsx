const Weather = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <p>Weather component!</p>
    </div>
  );
};

export default Weather;
